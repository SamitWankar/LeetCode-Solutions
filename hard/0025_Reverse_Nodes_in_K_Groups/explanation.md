
### `explanation.md`

```markdown
# Explanation - Reverse Nodes in K-Group

## Core Idea

The linked list is divided into groups of `k` nodes.

Each complete group is reversed independently.

If the last group contains fewer than `k` nodes, it is left unchanged.

For example:

```text
1 → 2 → 3 → 4 → 5
k = 3